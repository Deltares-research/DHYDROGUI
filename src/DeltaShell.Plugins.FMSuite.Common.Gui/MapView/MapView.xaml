<UserControl x:Class="DeltaShell.Plugins.FMSuite.Common.Gui.MapView.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeltaShell.Plugins.FMSuite.Common.Gui.MapView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             d:DataContext="{d:DesignInstance {x:Type local:MapViewModel}}"
             local:ActualSizeObserver.Observe="True"
             local:ActualSizeObserver.ObservedHeight="{Binding MapHeight, Mode=OneWayToSource}"
             local:ActualSizeObserver.ObservedWidth="{Binding MapWidth, Mode=OneWayToSource}">
    <Image Source="{Binding Path=MapImage}"
           Width="auto"
           Height="auto"
           Stretch="Fill">
    </Image>
</UserControl>
