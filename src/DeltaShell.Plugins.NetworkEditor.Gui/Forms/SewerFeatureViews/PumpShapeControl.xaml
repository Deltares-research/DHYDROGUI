<UserControl x:Class="DeltaShell.Plugins.NetworkEditor.Gui.Forms.SewerFeatureViews.PumpShapeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeltaShell.Plugins.NetworkEditor.Gui.Forms.SewerFeatureViews"
             mc:Ignorable="d" 
             d:DesignHeight="10" d:DesignWidth="10">
    <UserControl.DataContext>
        <local:PumpShapeControlViewModel x:Name="ViewModel"/>
    </UserControl.DataContext>
    <Grid x:Name="ViewGrid" SizeChanged="ViewGrid_OnSizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Canvas>
            <!-- Suction -->
            <!-- Connector -->
            <Polyline Points="{Binding SuctionConnection}" Stroke="Black" StrokeThickness="2"/>

            <!-- Start -->
            <Polyline Points="{Binding SuctionStartLevel}" 
                  Stroke="ForestGreen" StrokeThickness="2"/>

            <!-- Stop -->
            <Polyline Points="{Binding SuctionStopLevel}" 
                  Stroke="Red" StrokeThickness="2"/>

            <!-- Delivery -->
            <!-- Connector -->
            <Polyline Points="{Binding Connection}" Stroke="Black" StrokeThickness="2"/>

            <!-- Start -->
            <Polyline Points="{Binding DeliveryStartLevel}" 
                  Stroke="ForestGreen" StrokeThickness="2"/>

            <!-- Stop -->
            <Polyline Points="{Binding DeliveryStopLevel}" 
                  Stroke="Red" StrokeThickness="2"/>

            <!-- Pump -->
            <Rectangle Canvas.Left="{Binding IconLeftOffset}" Canvas.Top="{Binding IconTopOffset}"
                       Width="16" Height="16">
                <Rectangle.Fill>
                    <ImageBrush ImageSource="../../Resources/PumpSmall.png"/>
                </Rectangle.Fill>
            </Rectangle>
        </Canvas>
    </Grid>
</UserControl>
