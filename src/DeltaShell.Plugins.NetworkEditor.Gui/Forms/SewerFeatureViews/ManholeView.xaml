<UserControl x:Class="DeltaShell.Plugins.NetworkEditor.Gui.Forms.SewerFeatureViews.ManholeView"
             x:Name="UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeltaShell.Plugins.NetworkEditor.Gui.Forms.SewerFeatureViews"
             xmlns:hydro="clr-namespace:DelftTools.Hydro;assembly=DelftTools.Hydro"
             xmlns:structures="clr-namespace:DelftTools.Hydro.Structures;assembly=DelftTools.Hydro"
             xmlns:structureFeatureView="clr-namespace:DeltaShell.Plugins.NetworkEditor.Gui.Forms.StructureFeatureView"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000" d:DataContext="{d:DesignInstance local:ManholeViewModel}">
    <UserControl.DataContext>
        <local:ManholeViewModel x:Name="ViewModel" />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,3,3,3"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="0,3,3,3"/>
        </Style>
        <Style TargetType="ListBoxItem" x:Key="ListBoxStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border Name="ItemBorder"
                                BorderBrush="Black" 
                                BorderThickness="1"
                                Background="White">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FFD0ECFD"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="LightSkyBlue"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="2"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel >
            <!-- General info -->
            <Expander IsExpanded="True">
                <Expander.Header>
                    <TextBlock Text="General info" FontWeight="Bold"/>
                </Expander.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Width="300" Grid.Row="1" HorizontalAlignment="Left">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <!-- Row 0 -->
                        <Label Grid.Column="0" Grid.Row="0" Content="Name"/>
                        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Manhole.Name}"/>

                        <!-- Row 1 -->
                        <Label Grid.Column="0" Grid.Row="1" Content="ID"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Manhole.Id}"/>

                        <!-- Row 2 -->
                        <Label Grid.Column="0" Grid.Row="2" Content="Compartments"/>
                        <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding Manhole.Compartments.Count}"/>

                        <!-- Row 3 -->
                        <Label Grid.Column="0" Grid.Row="3" Content="X-Coordinate"/>
                        <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding Manhole.XCoordinate}"/>

                        <!-- Row 4 -->
                        <Label Grid.Column="0" Grid.Row="4" Content="Y-Coordinate"/>
                        <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding Manhole.YCoordinate}"/>

                        <!-- Row 5 -->
                        <Label Grid.Column="0" Grid.Row="5" Content="Surface level [m]"/>
                        <TextBlock Grid.Column="1" Grid.Row="5" Text="{Binding SurfaceLevel}"/>

                        <!-- Row 6 -->
                        <Label Grid.Column="0" Grid.Row="6" Content="Bottom level [m]"/>
                        <TextBlock Grid.Column="1" Grid.Row="6" Text="{Binding BottomLevel}"/>
                    </Grid>

                    <!-- Manhole overview -->
                    <Border Grid.Column="0" BorderBrush="#FF8B8B8B" BorderThickness="1" Margin="7,7,0,5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <local:ManholeVisualisation Background="#FFE8E8E8"
                                                        Manhole="{Binding DataContext.Manhole, RelativeSource={RelativeSource AncestorType={x:Type local:ManholeView}}}"
                                                        SelectedItem="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type local:ManholeView}}}"
                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                        Width="700" Height="500"/>
                            <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
                                <Button Content="Add compartment" Command="{Binding AddCompartmentCommand}" Margin="5,3,3,3"/>
                                <Button Content="Add orifice" Command="{Binding AddOrificeCommand}" Margin="5,3,3,3"/>
                                <Button Content="Add pump" Command="{Binding AddPumpCommand}" Margin="5,3,3,3"/>
                                <Button Content="Add weir" Command="{Binding AddWeirCommand}" Margin="5,3,3,3"/>
                                <Button Content="Remove item" Command="{Binding RemoveCompartmentCommand}" Margin="0,3,3,3"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <ContentControl Grid.Column="3" Content="{Binding SelectedItem}" x:Name="ContentControl" VerticalContentAlignment="Top">
                        <ContentControl.Resources>
                            <DataTemplate DataType="{x:Type hydro:Compartment}">
                                <local:CompartmentView Compartment="{Binding Content, ElementName=ContentControl}"/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type structures:Weir}">
                                <Rectangle Fill="LimeGreen" Width="32" Height="32"/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type structures:Pump}">
                                <Rectangle Fill="HotPink" Width="32" Height="32"/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type structures:Orifice}">
                                <Rectangle Fill="Red" Width="32" Height="32"/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type structures:Pipe}">
                                <Rectangle Fill="Blue" Width="32" Height="32"/>
                            </DataTemplate>
                        </ContentControl.Resources>
                    </ContentControl>
                </Grid>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
